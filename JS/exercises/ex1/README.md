# Exercise 1 — Tách module (theme + format)

Files:
- `theme.js` — named exports: `VALID_THEMES`, `getTheme`, `setTheme`
- `format.js` — default export object with `formatDate`, `formatCurrency`
- `app.js` — demo importer

Run in browser with a local server and include `app.js` as module, or open via devtools console.
