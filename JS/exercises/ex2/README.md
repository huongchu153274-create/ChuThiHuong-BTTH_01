# Exercise 2 — Alert component

Files:
- `components/Alert.js` — default export `showAlert`, named `ALERT_TYPES`
- `components/index.js` — re-export barrel (optional)
- `app.js` — demo that creates a button and shows an alert

Open `app.js` as a module in an HTML page or run with a local server.
